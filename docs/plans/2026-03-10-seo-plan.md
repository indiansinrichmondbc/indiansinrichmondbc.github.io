# SEO Plan: Rank for "indians in richmond bc"

**Date**: 2026-03-10
**Goal**: Make `indiansinrichmondbc.github.io` index reliably and rank for local intent queries such as `indians in richmond bc`, `indian businesses richmond bc`, and related terms.

## What was implemented in this repo
1. Added canonical site configuration and locale settings in `_config.yml`.
2. Enabled `jekyll-sitemap` plugin to generate `sitemap.xml`.
3. Added a crawlable `robots.txt` that points to sitemap.
4. Added optional Google Search Console verification meta support.
5. Added structured data (`WebSite` JSON-LD) in `<head>`.
6. Strengthened homepage title/description and introductory keyword context.

## Immediate actions (today)
1. Deploy the latest changes to GitHub Pages.
2. Open Google Search Console and add property: `https://indiansinrichmondbc.github.io`.
3. Add the verification token in `_config.yml` under `google_site_verification`.
4. In Search Console, submit:
- `https://indiansinrichmondbc.github.io/sitemap.xml`
- Request indexing for `https://indiansinrichmondbc.github.io/`

## Content plan (next 2 weeks)
1. Create category landing pages with unique content:
- `/restaurants-richmond-bc`
- `/indian-grocery-richmond-bc`
- `/indian-temples-richmond-bc`
- `/indian-services-richmond-bc`
2. Add at least 250-400 words of unique, useful copy per page.
3. Add internal links between homepage and category pages.
4. Add business details where available (address area, phone, website) to improve trust and relevance.

## Authority and local signals
1. Link this site from your Facebook group About section, Instagram bio, and WhatsApp group description.
2. Ask 5-10 community partners to link back (temple pages, local groups, businesses).
3. Keep NAP consistency where possible for listed businesses.

## Technical checks
1. Ensure no accidental `noindex` headers/tags.
2. Ensure homepage response is 200 and renderable without JS.
3. Keep pages fast (compress images, avoid oversized assets).
4. Keep one clear H1 per page and keyword-focused title tag.

## Expectations
1. Indexing can start within days but ranking movement often takes 2-8 weeks.
2. For a competitive local query, consistent content updates and backlinks are usually required.
3. Search Console coverage and performance reports should be reviewed weekly.
